
void main(List<String> arguments) {
  
    // Sum of two numbres
    var one = 1;
    var two = 2;

    var sum = one + two;

    print(sum.toString());

    // Tempature Converter

    var f = 120;

    var fahrenheitToCelsiusTempFormula = (f - 32) * (5/9);

    print(fahrenheitToCelsiusTempFormula.toString());
}
